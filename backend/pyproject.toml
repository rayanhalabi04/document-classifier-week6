[project]
name = "document-classifier-service"
version = "0.1.0"
description = "Authenticated document classification service"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
]

[dependency-groups]
dev = [
    "pytest",
    "httpx",
]
