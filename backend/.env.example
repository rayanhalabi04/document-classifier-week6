<<<<<<< HEAD
# Copy this file to .env and fill in values before running locally.
# Never commit .env to Git.

# --- Database ---
DATABASE_URL=postgresql://docuser:docpass@localhost:5432/docclassifier

# --- JWT ---
JWT_SECRET=change-this-to-a-long-random-string

# --- MinIO ---
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET_ORIGINALS=originals
MINIO_BUCKET_OVERLAYS=overlays

# --- Redis ---
REDIS_URL=redis://localhost:6379/0

# --- Vault ---
VAULT_ADDR=http://localhost:8200
VAULT_TOKEN=root

# --- SFTP ---
SFTP_HOST=localhost
SFTP_PORT=2222
SFTP_USERNAME=user
SFTP_PASSWORD=pass
SFTP_DROP_DIR=/upload

# --- Logging ---
LOG_LEVEL=INFO
LOG_FORMAT=console
=======
# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=document_classifier
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Redis
REDIS_URL=redis://redis:6379/0

# MinIO
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_ENDPOINT=minio:9000

# Vault
VAULT_ADDR=http://vault:8200
VAULT_TOKEN=root

# SFTP
SFTP_HOST=sftp
SFTP_PORT=22
SFTP_USER=vendor
SFTP_PASSWORD=vendorpass

# JWT
JWT_SECRET=change-me-in-production

# pgAdmin
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=admin
>>>>>>> upstream/main
